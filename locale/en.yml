fof-oauth:
  admin:
    settings:
      providers:
        clerk:
          description: Register your forum with SSO {link}

          client_id_label: => fof-oauth.ref.settings.client_id
          client_secret_label: => fof-oauth.ref.settings.client_secret

  forum:
    log_in:
      with_clerk_button: Sign in 

    providers:
      clerk: '=> fof-oauth.lib.providers.clerk'

  lib:
    providers:
      clerk: Clerk
